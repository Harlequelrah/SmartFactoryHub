 # for attr in self.attrs:
        #     final_dict=data_dict1.copy()
        #     del final_dict[attr]
        #     x = [
        #         "year",
        #         "month",
        #         "day",
        #         "hour",
        #         "minute",
        #     ]
        #     for i in self.attrs:
        #         x.append(attr)
        #     df = pd.DataFrame(
        #     [
        #         final_dict|data_dict2
        #     ]
        # )
        #     X = df[x]
        #     predicted_data=self.models[attr].predict(X)[0]
        #     logger.info(
        #     f"{attr} prédite pour dans 10 secondes ({future_timestamp}):{predicted_data}"
        # )
        # match (attr):
        #     case "temperature":
        #         self.check_temperature(final_dict,data_dict2,predicted_data)
        # case "pressure":self.check_pressure(predicted_data)
        # case "humidity":self.check_humidity(predicted_data)
        # case "luminosity":self.check_luminosity(predicted_data)
        # case "energy":self.check_energy(predicted_data)
